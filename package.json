{
  "name": "dashview",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.0",
    "happy-dom": "^15.0.0",
    "sortablejs": "^1.15.6",
    "vitest": "^2.0.0"
  }
}
